{
  "name": "app-english",
  "private": true,
  "scripts": {
    "dev": "npm run web:dev",
    "web:dev": "npm --prefix frontend/web_app run dev",
    "web:build": "npm --prefix frontend/web_app run build",
    "web:start": "npm --prefix frontend/web_app run start",
    "web:typecheck": "npm --prefix frontend/web_app run typecheck"
  }
}
