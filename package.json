{
  "name": "app-english",
  "private": true,
  "scripts": {
    "web:dev": "npm --prefix frontend/web_app run dev",
    "web:build": "npm --prefix frontend/web_app run build",
    "api:dev": "npm --prefix backend run dev",
    "api:generate": "npm --prefix backend run db:generate",
    "api:push": "npm --prefix backend run db:push",
    "api:seed": "npm --prefix backend run db:seed"
  }
}
